summary: Get all fleet
description: Get all fleet
security:
  - ApiKeyAuth: []
responses:
  '200':
    description: Fleet list
  '500':
    description: Error generic
    content:
      application/json:
        schema:
          $ref: "../../schemas/Error.yaml"
